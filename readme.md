# Simple MVC with Express + EJS + TailwindCSS with livereload

## need 2 terminals for development

```bash
yarn css //for tailwind refresh
```

```bash
yarn dev //for ejs refresh
```

...wanna know to do this with 1 command

## build
```bash
yarn build
```
build html to dist directory.  
css is always built by `yarn css`